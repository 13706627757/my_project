# Jetson Nano 专用 requirements.txt
# 先安装系统依赖
# sudo apt-get update
# sudo apt-get install python3-pip python3-dev

# Python 包
numpy
pillow
# pyqt5 使用系统 apt 安装，不要用 pip 在 Jetson 上安装
# sudo apt-get install python3-pyqt5
pyserial
opencv-python

# PyTorch for Jetson Nano (JetPack 4.x)
# 从 NVIDIA 官方源安装
# pip install torch==1.7.0+cu102 torchvision==0.8.1+cu102 torchaudio==0.7.0 -f https://download.pytorch.org/whl/torch_stable.html

# ===== Jetson Nano 依赖 =====
# 使用 ONNX 推理（不需要 PyTorch），轻量级
# 只需要 onnxruntime，不需要 torch 或 ultralytics

# 通过 pip 安装
numpy
pillow
opencv-python
pyserial
# onnxruntime  # 需要手动安装：pip install onnxruntime

# GUI （通过 apt 安装更快）
# sudo apt-get install python3-pyqt5

# ===== 说明 =====
# 1. 首先转换 .pt 模型到 ONNX：在 Windows 上运行 convert_pt_to_onnx.py
# 2. 将生成的 ONNX 模型上传到 Jetson Nano 的 ONNX/ 目录
# 3. 在 Jetson Nano 上安装依赖：
#    pip3 install numpy pillow opencv-python pyserial
#    pip3 install onnxruntime
#    sudo apt-get install python3-pyqt5
# 4. 运行应用：python3 multi.py